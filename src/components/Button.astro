---
const { variant= "primary", size = "medium" } = Astro.props; 
const buttonStyling = "p-2 "; 
const variantClasses = {

"primary": "bg-rose-100 hover:bg-diner-100 border-5 border-rose-100 shadow-none",

};
const sizeClasses = {

"medium": "scale-150",
"long": "scale-150 px-10",

"small": "scale-50",

};

---
<div>
    <button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot /></button>
</div>